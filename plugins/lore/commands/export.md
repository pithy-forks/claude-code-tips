---
description: export the lore knowledge graph to portable formats (json, csv, markdown)
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion
---
<!-- tested with: claude code v2.1.122 -->

Read the skill file at `${CLAUDE_PLUGIN_ROOT}/skills/export/SKILL.md` and follow its instructions completely to handle this `/lore:export` request. The user's intent comes from whatever they typed after `/lore:export`.
